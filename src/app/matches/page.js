import CardResultDetails from "../../components/card/resultDetail/cardResultDetail";

export default function MatchesPage() {
  return (
    <main className="min-h-screen">
      <h2 className="font-semibold mb-4">Matches result</h2>
      <div>
        <CardResultDetails />
      </div>
    </main>
  );
}
